# Secrets & local config — NEVER commit
.env
.env.local
*.secret
secrets.json
credentials.json

# Dependencies / runtime
node_modules/
npm-debug.log*
.DS_Store
debug/

# Golden labeled set for accuracy measurement (personal merchant names)
golden.json
